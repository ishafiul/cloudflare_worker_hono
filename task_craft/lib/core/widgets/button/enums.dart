/// enum for button type
enum ButtonType {
  base,
  primary,
  success,
  warning,
  danger,
}

enum ButtonFill { solid, outline, none }

enum ButtonShape { rounded, base, rectangular }

enum ButtonSize { large, middle, mini, small }
